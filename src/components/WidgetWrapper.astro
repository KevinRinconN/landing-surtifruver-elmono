---
import { cn } from '@/lib/utils';

export interface Props {
  class?: string;
}

const { class: otherClass } = Astro.props;
---

<section class={cn("max-w-7xl w-full p-3 md:px-6 mx-auto my-20", otherClass)}>
  <div class="relative w-full">
    <slot />
  </div>
</section>
